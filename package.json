{
  "name": "cortex",
  "displayName": "Cortex",
  "description": "A local, private coding agent for VS Code — like Cline, but powered entirely by your own Ollama models. No API keys, no cloud.",
  "version": "0.1.0",
  "publisher": "cortex",
  "license": "MIT",
  "type": "module",
  "main": "./src/extension.cjs",
  "bin": {
    "cortex-cli": "./bin/cortex-cli.cjs"
  },
  "icon": "media/icon.png",
  "engines": {
    "vscode": "^1.85.0",
    "node": ">=18"
  },
  "categories": [
    "Machine Learning",
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "ollama",
    "ai",
    "agent",
    "cline",
    "local llm",
    "coding assistant",
    "chat"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/cortex/cortex"
  },
  "activationEvents": [],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "cortex",
          "title": "Cortex",
          "icon": "media/activitybar.svg"
        }
      ]
    },
    "views": {
      "cortex": [
        {
          "type": "webview",
          "id": "cortex.chatView",
          "name": "Chat"
        }
      ]
    },
    "commands": [
      {
        "command": "cortex.newChat",
        "title": "New Chat",
        "category": "Cortex",
        "icon": "$(add)"
      },
      {
        "command": "cortex.selectModel",
        "title": "Select Model",
        "category": "Cortex",
        "icon": "$(list-selection)"
      },
      {
        "command": "cortex.openChat",
        "title": "Open Chat",
        "category": "Cortex"
      },
      {
        "command": "cortex.toggleAutoApprove",
        "title": "Toggle Auto-Approve",
        "category": "Cortex"
      },
      {
        "command": "cortex.showLogs",
        "title": "Show Logs",
        "category": "Cortex"
      },
      {
        "command": "cortex.revertLastTurn",
        "title": "Revert Last Turn (all files)",
        "category": "Cortex"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "cortex.newChat",
          "when": "view == cortex.chatView",
          "group": "navigation@1"
        },
        {
          "command": "cortex.selectModel",
          "when": "view == cortex.chatView",
          "group": "navigation@2"
        }
      ]
    },
    "configuration": {
      "title": "Cortex",
      "properties": {
        "cortex.host": {
          "type": "string",
          "default": "http://localhost:11434",
          "description": "Base URL of your local (or remote) Ollama server."
        },
        "cortex.model": {
          "type": "string",
          "default": "qwen2.5-coder:3b",
          "description": "Ollama model to use for the agent — must exactly match a locally pulled model name/tag (see `ollama list`)."
        },
        "cortex.autoApprove": {
          "type": "boolean",
          "default": false,
          "description": "Automatically approve file writes/edits and shell commands without asking for confirmation. Use with caution. Equivalent to approvalPolicy \"full-auto\"."
        },
        "cortex.approvalPolicy": {
          "type": "string",
          "enum": ["suggest", "auto-edit", "full-auto"],
          "default": "suggest",
          "description": "Codex-CLI-style approval tiers. \"suggest\": confirm every mutating tool (safest). \"auto-edit\": file writes/edits/deletes run without confirmation, shell commands still ask. \"full-auto\": nothing asks for confirmation."
        },
        "cortex.fastModel": {
          "type": "string",
          "default": "",
          "description": "Optional smaller/faster model used for read-only investigation steps (read_file, list_dir, search_code) — the main model is used once the agent needs to write code or run commands. Leave empty to always use the main model."
        },
        "cortex.sandboxCommands": {
          "type": "boolean",
          "default": false,
          "description": "Run run_command/run_background inside a bubblewrap sandbox (Linux only, requires bwrap installed) — the rest of the filesystem is mounted read-only and network is disabled by default. Falls back to unsandboxed execution with a log note if bwrap isn't available."
        },
        "cortex.sandboxAllowNetwork": {
          "type": "boolean",
          "default": false,
          "description": "Allow network access from inside the command sandbox (only relevant when cortex.sandboxCommands is on)."
        },
        "cortex.autoApproveCommands": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Command prefixes (e.g. \"npm test\", \"git status\") that run_command may execute without confirmation, even when cortex.autoApprove is off. A command is allowed if it starts with one of these prefixes."
        },
        "cortex.provider": {
          "type": "string",
          "enum": ["ollama", "openai-compatible"],
          "default": "ollama",
          "description": "Which backend to send chat requests to. \"openai-compatible\" works with OpenAI itself, OpenRouter, LM Studio, vLLM, or any server exposing /v1/chat/completions."
        },
        "cortex.apiKey": {
          "type": "string",
          "default": "",
          "description": "API key sent as a Bearer token when cortex.provider is \"openai-compatible\". Not used for Ollama."
        },
        "cortex.webhookUrl": {
          "type": "string",
          "default": "",
          "description": "Slack or Discord incoming-webhook URL the agent's notify_webhook tool can post short status updates to."
        },
        "cortex.mcpServers": {
          "type": "array",
          "default": [],
          "description": "MCP (Model Context Protocol) servers to connect to at startup. Each server's tools are added to the agent's toolset, prefixed \"mcp_<name>_\".",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "command": { "type": "string" },
              "args": { "type": "array", "items": { "type": "string" } },
              "env": { "type": "object" }
            },
            "required": ["name", "command"]
          }
        },
        "cortex.schedules": {
          "type": "array",
          "default": [],
          "description": "Cron-based recurring agent runs (e.g. daily TODO summaries, dependency checks). Runs unattended in read-only Plan Mode unless autoApprove is set on the schedule.",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "cron": { "type": "string", "description": "Standard 5-field cron expression, e.g. \"0 9 * * 1-5\"." },
              "prompt": { "type": "string" },
              "autoApprove": { "type": "boolean", "default": false }
            },
            "required": ["cron", "prompt"]
          }
        },
        "cortex.temperature": {
          "type": "number",
          "default": 0.2,
          "description": "Sampling temperature passed to the model."
        },
        "cortex.maxSteps": {
          "type": "number",
          "default": 25,
          "description": "Maximum number of tool-call steps the agent may take per request before stopping."
        },
        "cortex.contextBudgetTokens": {
          "type": "number",
          "default": 6000,
          "description": "Rough token budget for conversation history sent to the model each turn (chars/4 heuristic, no tokenizer dependency). Lower this for small-context-window models; when exceeded, the oldest messages are dropped and the model is told so explicitly rather than silently losing context."
        }
      }
    }
  },
  "scripts": {
    "package": "vsce package --no-dependencies",
    "publish": "vsce publish --no-dependencies",
    "test": "node --test test/**/*.test.cjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^2.32.0",
    "typescript": "^5.9.3"
  }
}
