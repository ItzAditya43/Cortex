{
  "name": "agent-os-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Agent OS - Autonomous Coding Agent (Monorepo)",
  "scripts": {
    "compile": "cd extension && npm run compile",
    "install:all": "cd extension && npm install && cd ../webview && npm install",
    "build:extension": "cd extension && npm run compile",
    "build:webview": "cd webview && npm run build",
    "build": "npm run build:webview && npm run build:extension",
    "dev:backend": "cd backend && poetry run uvicorn src.main:app --reload --port 8080"
  },
  "workspaces": [
    "extension",
    "webview"
  ]
}